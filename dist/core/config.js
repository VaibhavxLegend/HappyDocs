"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.getConfig = getConfig;
const vscode = __importStar(require("vscode"));
const defaults = {
    include: [
        "src/**/*.{ts,tsx,js,jsx}",
        "app/**/*.{ts,tsx,js,jsx}",
        "routes/**/*.{ts,tsx,js,jsx}",
        "controllers/**/*.{ts,tsx,js,jsx}"
    ],
    exclude: [
        "**/node_modules/**",
        "**/dist/**",
        "**/build/**",
        "**/coverage/**",
        "**/*.test.*",
        "**/*.spec.*",
        "**/*.generated.*"
    ],
    frameworks: ["express", "nestjs"],
    outputDirectory: "docs/api",
    openapiFormat: "yaml",
    apiTitle: "My API",
    apiVersion: "1.0.0",
    apiDescription: "API documentation generated by HappyDocs.",
    baseUrl: "http://localhost:3000",
    enableAiEnrichment: false,
    aiProvider: "openai",
    maxFiles: 2500
};
function getConfig(resource) {
    const value = vscode.workspace.getConfiguration("hybridApiDocs", resource);
    return {
        include: value.get("include", defaults.include),
        exclude: [...new Set([...defaults.exclude, ...value.get("exclude", [])])],
        frameworks: value.get("frameworks", defaults.frameworks),
        outputDirectory: value.get("outputDirectory", defaults.outputDirectory),
        openapiFormat: value.get("openapiFormat", defaults.openapiFormat),
        apiTitle: value.get("apiTitle", defaults.apiTitle),
        apiVersion: value.get("apiVersion", defaults.apiVersion),
        apiDescription: value.get("apiDescription", defaults.apiDescription),
        baseUrl: value.get("baseUrl", defaults.baseUrl),
        enableAiEnrichment: value.get("enableAiEnrichment", defaults.enableAiEnrichment),
        aiProvider: value.get("aiProvider", defaults.aiProvider),
        maxFiles: value.get("maxFiles", defaults.maxFiles)
    };
}
//# sourceMappingURL=config.js.map